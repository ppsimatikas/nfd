{
  "name": "nfd",
  "version": "1.0.0",
  "description": "A Decentralized Big Data Infrastructure.",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "build": "yarn workspace ui run build",
    "start:ui": "yarn workspace ui run start",
    "start:backend": "firebase emulators:start",
    "start": "yarn start:backend & yarn start:ui",
    "verify": "yarn workspace ui run test"
  },
  "dependencies": {
    "firebase-tools": "^13.13.2"
  }
}
